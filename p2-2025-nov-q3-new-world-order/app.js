/* ═══════════════════════════════════════════════════════════════
   DATA BLOCK — replaced by build.py from a content JSON file.
   The build script reads a content payload (see references/content-schema.md)
   and substitutes this entire comment block with:
     const SRC = { ... };
     const QS = [ ... ];
     const MTA_ANSWERS = [ ... ];
   ═══════════════════════════════════════════════════════════════ */
const SRC = {
  '3A': {
    label: 'Source 3A',
    type: 'Historical Article',
    html: `<div class="sp-body"><em>The source below is an article titled 'BRICS Intergovernmental Organization', taken from records of the BRICS summit held in Kazan in Russia in October 2024. It focuses on the reasons for the formation of BRICS and its history since 2006.</em><br><br>The first meeting in the BRIC format took place on 20 September 2006 on the side-lines of the 61st session of the UN (United Nations) General Assembly in New York. The meeting was attended by the ministers of foreign affairs of Russia, Brazil and China and the minister of defence of India, who all agreed to develop multifaceted (multi-sided) cooperation between the four countries.<br><br>The BRIC summit took place on 16 June 2009 in Yekaterinburg in Russia. The final Joint Statement stated the desire of BRIC countries to develop 'an incremental (slowly increasing), proactive (hands-on), pragmatic (practical), open and transparent dialogue and cooperation' that is 'conducive (suited) not only to serving common interests of emerging market economies and developing countries, but also to building a harmonious (friendly) world of lasting peace and common prosperity'.<br><br>With the accession (joining) of the Republic of South Africa (the first summit with South Africa took place on 14 April 2011 in Sanya, China), the abbreviation 'BRICS' was established as the name of the association. One of the priorities of BRICS activities is to promote the formation of a multi-polar (many-centred) democratic world and strengthen global security and stability around the world. The association consistently advocates (encourages) for compliance (agreement) with the principles of international law with the UN in the central role, and the rejection of unilateral coercive (forceful) measures. On 1 January 2024, Egypt, Iran, the United Arab Emirates, Saudi Arabia, and Ethiopia joined BRICS.</div>`,
    attr: 'From \'BRICS Intergovernmental Organization\', the Ministry of Foreign Affairs of the Russian Federation, 2024'
  },
  '3B': {
    label: 'Source 3B',
    type: 'Historical Article',
    html: `<div class="sp-body"><em>The source below is from an article titled, 'The BRICS Challenge to the G7 Established International Order' by MG Mooradian, published by the Foreign Policy Research Institute on 20 September 2024. It highlights the strategies adopted by BRICS to challenge the Global North.</em><br><br>BRICS is launching (starting) three strategic initiatives that aim to challenge the established international order: an artificial intelligence (AI) governance framework, an independent economic system and regional security coalitions (alliances). BRICS's current goal is to reshape the international order to allow nations to act independently, without involvement or enforcement from Western countries.<br><br>One of BRICS's primary objectives is to establish an independent payment system using member currencies to promote de-dollarisation (replace the US dollar in international trade), thereby diminishing (reducing) the impact of Western sanctions. The first step of this process is the attempt to influence specific vital international markets to gain economic leverage (advantage). One example of this new strategy is BRICS's involvement in the energy market, with the organisation aiming to create an energy partnership between its members. With the recent inclusion of energy-rich nations such as Saudi Arabia, Iran and the United Arab Emirates, BRICS could achieve a level of influence in the global energy market comparable to that of OPEC (Organisation of the Petroleum Exporting Countries), potentially altering the dynamics by controlling a significant portion of the world's oil and gas supplies.<br><br>Another critical market BRICS is attempting to influence is the agricultural sector, whose first objective is to create an independent grain trading system that was introduced by Russia. This initiative would increase the organisation's negotiating power in grain prices and combat sanctions by the United States and its G7 partners (other wealthy Western nations). BRICS seeks to alter (change) the existing global power structure which could result in diminished (reduced) US influence and the destabilisation of the international order.</div>`,
    attr: 'From \'The BRICS Challenge to the G7 Established International Order\' by MG Mooradian, Foreign Policy Research Institute, 2024'
  },
  '3C': {
    label: 'Source 3C',
    type: 'Graph',
    html: `<div class="sp-body"><em>The graph below, titled 'The Rise of the BRICS', was produced by STATISTA, an online market research company, from the International Monetary Fund (IMF) world economic outlook figures. It shows a comparative analysis of the growth of GDP (gross domestic product) between the G7 and BRICS countries covering the period from 1995 to 2023.</em></div><img class="sp-img" src="3C.png" alt="Infographic titled 'The Rise of the BRICS' comparing the G7 and BRICS countries' share of global GDP at purchasing power parity for the years 1995, 2010 and 2023, shown as two stacked flag-filled bars per year. Labelled percentages: G7 shows 44,9% in 1995, 34,3% in 2010 and 29,6% in 2023; BRICS shows 16,9% in 1995, 26,6% in 2010 and 32,1% in 2023. Side labels list the G7 countries (Canada, Britain, France, Italy, Germany, Japan, USA) and the BRICS countries (South Africa, Brazil, Russia, India, China).">`,
    attr: 'From https://www.statista.com/chart/30638/brics-and-g7-share-of-global-gdp/. Accessed on 5 March 2025.'
  },
  '3D': {
    label: 'Source 3D',
    type: 'Newspaper Article',
    html: `<div class="sp-body"><em>The extract below was taken from an article, titled 'How Trump's Dump on De-dollarisation Affects BRICS' by K Wadhwa, published in the Responsible Statecraft Weekly newsletter on 28 November 2024. It includes an extract from a speech that Trump delivered as president-elect during a campaign rally in Wisconsin in September 2024, highlighting how BRICS poses a threat to the balance of power between the Global North and the Global South.</em><br><br>'Many countries are leaving the dollar. They are not going to leave the dollar with me. I'll say, you leave the dollar, you are not doing business with the United States because we're going to put a 100% tariff (tax) on your growth.' He was referring to BRICS+ countries, many of whom are critical of the 'rules-based' international order, established by the United States in the Bretton Woods era, which they see as benefitting the West (especially the United States) over the emerging Global South.<br><br>De-dollarisation may not happen anytime soon but the fact that the BRICS+ countries, which form 35% of the world's GDP and 45% of the world's population, appear to be taking it seriously, is a real concern for American leaders, including Trump.<br><br>In his previous presidential term, Trump started a trade war with China in addition to unleashing a new torrent of sanctions on Iran, Venezuela and Russia. If he (Trump) doubles down on using sanctions as a weapon, more countries will want to hedge their bets. After all they might also be on the receiving end of sanctions if they were to fall out of favour with the United States one day. 'That will mean more consolidation (strengthening) of China's leadership of the BRICS+ and more innovation in terms of financial infrastructure, co-operation and integration ...'<br><br>He [Trump] noted that even Western-friendly countries, like Turkey, wish to join the alliance (BRICS+).</div>`,
    attr: 'From Responsible Statecraft Weekly newsletter, 28 November 2024'
  },
  'compare': {
    label: 'Sources 3C + 3D',
    type: 'Comparison',
    html: `<div class="sp-body"><strong>Source 3D</strong> (extract)<br><br>'Many countries are leaving the dollar &hellip; you leave the dollar, you are not doing business with the United States because we're going to put a 100% tariff on your growth.' &hellip; the fact that the BRICS+ countries, which form 35% of the world's GDP and 45% of the world's population, appear to be taking it seriously, is a real concern for American leaders, including Trump.</div><div class="sp-body" style="margin-top:12px;"><strong>Source 3C</strong> &mdash; 'The Rise of the BRICS' (G7 versus BRICS share of global GDP, 1995&ndash;2023):</div><img class="sp-img" src="3C.png" alt="Infographic comparing the G7 and BRICS share of global GDP for 1995, 2010 and 2023. G7 falls from 44,9% to 34,3% to 29,6%; BRICS rises from 16,9% to 26,6% to 32,1%.">`,
    attr: 'Re-read both sources, then explain how the evidence in Source 3C supports the information in Source 3D.'
  },
  'para': {
    label: 'Paragraph Task',
    type: 'Q3.6, 8 marks',
    html: `<div class="sp-body">Now build the 8-mark paragraph. Using the relevant sources and your own knowledge, explain how the expansion of BRICS countries might be seen as a threat to the balance of power between the Global North (Western countries) and the Global South (BRICS countries).<br><br>Work through it in four steps: a clear topic sentence, specific evidence from the sources, one piece of your own knowledge, and a conclusion that answers the question.</div>`,
    attr: 'Q3.6 &mdash; write about EIGHT lines (about 80 words).'
  },
  'mta': {
    label: 'Mark This Answer',
    type: 'Evaluative Task',
    html: `<div class="sp-body">Two learners wrote a paragraph answering Q3.6. Read each answer and mark it against the six criteria of a strong history paragraph. For each criterion, decide whether the answer does it fully, partially, or not at all.</div>`,
    attr: 'Bonus round &mdash; unlocked at 70%.'
  },
};

const QS = [
  {id: 'q1', src: '3A', num: '3.1.1', m: 1, t: 'When, according to the source, did the first meeting in the BRIC format take place?', lv: 1, ll: 'Extraction', opts: ['On 20 September 2006', 'On 16 June 2009', 'On 14 April 2011', 'On 1 January 2024'], c: 0, fb: 'The source states the first BRIC-format meeting took place on 20 September 2006 at the UN General Assembly. The other dates are also in the source but mark the 2009 Yekaterinburg summit, South Africa\'s 2011 accession, and the 2024 expansion.'},
  {id: 'q2', src: '3A', num: '3.1.2', m: 4, t: 'Name the FOUR countries in the source who agreed to develop multifaceted cooperation among themselves in the formation of BRIC. (Select FOUR.)', lv: 1, ll: 'Extraction', multi: true, mx: 4, opts: ['Russia', 'Brazil', 'China', 'India', 'South Africa', 'Egypt'], c: [0, 1, 2, 3], fb: 'The four founding BRIC countries named in the source are Russia, Brazil, China and India. South Africa joined only in 2011 (making it BRICS), and Egypt was one of the members added on 1 January 2024.'},
  {id: 'q3', src: '3A', num: '3.1.3', m: 2, t: 'Define the term summit in your own words.', lv: 1, ll: 'Concept', opts: ['A summit is a high-level meeting or conference between the heads of government or leaders of different countries.', 'A summit is a written treaty that binds several countries together in a permanent military and trading alliance.', 'A summit is the highest committee inside the United Nations that has the sole power to impose economic sanctions.', 'A summit is a public protest held by ordinary citizens to demand changes to their own government\'s foreign policy.'], c: 0, fb: 'A summit is a top-level meeting of heads of government or state. Option 2 confuses a meeting with a treaty; option 3 invents a UN body; option 4 describes a protest - none capture a meeting of leaders.'},
  {id: 'q4', src: '3A', num: '3.1.4', m: 4, t: 'Using information in the source and your own knowledge, explain the significance of six new countries joining BRICS on 1 January 2024.', lv: 2, ll: 'Interpretation', opts: ['It expanded and strengthened BRICS, giving the Global South more collective weight to challenge the economic and political dominance of the Western Global North.', 'It weakened BRICS badly, because adding so many new members made it impossible for the group ever to agree on any common policy.', 'It showed that BRICS had abandoned its economic goals and had become a purely military alliance aimed only at defending its members\' borders.', 'It meant the new members immediately adopted the US dollar and the G7\'s trade rules in order to be accepted into the group.'], c: 0, fb: 'Expansion added economic and demographic weight to the Global South\'s challenge to the West. Option 2 assumes paralysis; option 3 wrongly makes BRICS purely military; option 4 contradicts BRICS\' de-dollarisation aim.'},
  {id: 'q5', src: '3B', num: '3.2.1', m: 2, t: 'Name any TWO strategic initiatives launched by BRICS in the source that aimed at challenging the established international order. (Select TWO.)', lv: 1, ll: 'Extraction', multi: true, mx: 2, opts: ['An artificial intelligence (AI) governance framework', 'An independent economic payment system', 'A single shared BRICS currency in circulation', 'A joint BRICS military invasion force', 'A common BRICS passport for travel'], c: [0, 1], fb: 'The source names three initiatives: an AI governance framework, an independent economic system and regional security coalitions. A single circulating currency, an invasion force and a common passport are not mentioned in the source.'},
  {id: 'q6', src: '3B', num: '3.2.2', m: 2, t: 'Explain the concept de-dollarisation in the context of the BRICS challenge to Western countries.', lv: 2, ll: 'Concept', opts: ['De-dollarisation means reducing reliance on the US dollar in international trade by using BRICS members\' own currencies instead, to weaken Western financial influence.', 'De-dollarisation means printing far more US dollars so that poorer countries can afford to buy American goods more cheaply and easily.', 'De-dollarisation means fixing the value of every BRICS currency permanently to the US dollar in order to keep exchange rates fully stable.', 'De-dollarisation means banning all paper money everywhere and moving the entire world economy over to a single digital cryptocurrency.'], c: 0, fb: 'De-dollarisation is cutting dependence on the dollar by trading in other currencies. Option 2 does the opposite (more dollars); option 3 ties currencies TO the dollar (the reverse); option 4 confuses it with abolishing cash.'},
  {id: 'q7', src: '3B', num: '3.2.3', m: 2, t: 'Identify ONE reason in the source why BRICS countries attempted to influence the agricultural sector.', lv: 1, ll: 'Extraction', opts: ['To create an independent grain trading system that would boost BRICS\' negotiating power over grain prices and help combat Western sanctions.', 'To give away free grain to developing countries in order to win their political support and votes at the United Nations.', 'To force farmers in the G7 countries to sell all their grain only to BRICS members at very low fixed prices.', 'To replace all farming in member states with new factories, since agriculture was seen as old-fashioned and no longer profitable.'], c: 0, fb: 'The source says BRICS aimed to create an independent grain trading system to raise its negotiating power and combat US and G7 sanctions. Options 2, 3 and 4 give aims that appear nowhere in the source.'},
  {id: 'q8', src: '3B', num: '3.2.4', m: 4, t: 'Comment on how the \'diminished (reduced) US influence\' would change the existing international order.', lv: 2, ll: 'Interpretation', opts: ['It would erode US and Western dominance, shifting the world towards a multi-polar order in which the Global South plays a far more powerful economic and political role.', 'It would make almost no difference at all, because the United States would simply be replaced by the United Kingdom as the single dominant world power.', 'It would force the United States to leave the United Nations altogether and hand its Security Council seat directly over to China.', 'It would strengthen the G7\'s grip on the world economy, since far fewer rivals would be left to challenge Western trade rules.'], c: 0, fb: 'Reduced US influence points to a multi-polar world with a stronger Global South. Option 2 just swaps one Western power for another; option 3 invents a UN exit; option 4 says the opposite of what the source implies.'},
  {id: 'q9', src: '3C', num: '3.3.1(a)', m: 2, t: 'What message is conveyed by the graph regarding the change in the G7\'s share of global gross domestic product (GDP) between 1995 and 2023?', lv: 2, ll: 'Interpretation', opts: ['The G7\'s share of global GDP fell steadily, from 44,9% in 1995 to 29,6% in 2023, showing the West\'s shrinking economic dominance.', 'The G7\'s share of global GDP stayed almost exactly the same across the whole period, showing that Western economic power remained completely stable.', 'The G7\'s share of global GDP rose sharply after the year 2010, showing that the Western economies were recovering their strength.', 'The G7\'s share of global GDP was always smaller than the BRICS share throughout the entire period shown on the graph.'], c: 0, fb: 'The graph shows a steady G7 decline from 44,9% to 29,6% - shrinking Western dominance. Option 2 denies the fall; option 3 reverses it; option 4 is false, since the G7 started far higher than BRICS.'},
  {id: 'q10', src: '3C', num: '3.3.1(b)', m: 2, t: 'What message is conveyed by the graph regarding the change in BRICS\' share of global GDP between 1995 and 2023?', lv: 2, ll: 'Interpretation', opts: ['BRICS\' share of global GDP rose steadily, from 16,9% in 1995 to 32,1% in 2023, showing the group\'s growing economic power.', 'BRICS\' share of global GDP declined slowly over the period, showing that the developing economies were falling further behind the West.', 'BRICS\' share of global GDP jumped suddenly in 1995 and then remained completely flat all the way until the year 2023.', 'BRICS\' share of global GDP overtook the G7\'s share as early as 1995, right at the very start of the period shown.'], c: 0, fb: 'The graph shows BRICS climbing from 16,9% to 32,1% - rising economic power. Option 2 reverses the trend; option 3 misreads the steady rise as flat; option 4 is false, since BRICS started at 16,9% versus the G7\'s 44,9%.'},
  {id: 'q11', src: '3C', num: '3.3.2', m: 4, t: 'Using the information in the source and your own knowledge, explain the implication of the growth in China\'s GDP from 1995 to 2023.', lv: 2, ll: 'Interpretation', opts: ['China\'s steady GDP growth positioned it as the leading economy within BRICS and an emerging superpower directly challenging the economic dominance of the United States.', 'China\'s GDP growth simply meant that it was able to export more cheap toys and clothing to Western shoppers with each passing year.', 'China\'s GDP growth showed that it had become fully dependent on the United States and the G7 countries for its own economic survival.', 'China\'s GDP growth had no real effect on world politics, because China chose to remain neutral and completely isolated from global affairs.'], c: 0, fb: 'China\'s rise makes it BRICS\' leading economy and a challenger to US dominance. Option 2 trivialises it to toys; option 3 reverses the power relationship; option 4 wrongly claims China stayed isolated.'},
  {id: 'q12', src: '3D', num: '3.4.1', m: 1, t: 'Who, according to the source, was Trump referring to when he said, \'... leave the dollar, you are not doing business with the US because we\'re going to put a 100% tariff on your growth\'?', lv: 1, ll: 'Extraction', opts: ['The BRICS+ countries', 'The G7 countries', 'The United Nations members', 'The OPEC oil producers'], c: 0, fb: 'The source states directly that \'He was referring to BRICS+ countries\'. The G7, the UN and OPEC are all named elsewhere in the sources but are not who Trump was threatening here.'},
  {id: 'q13', src: '3D', num: '3.4.2', m: 2, t: 'Give a reason in the source why BRICS+ countries are critical of the Bretton Woods era.', lv: 1, ll: 'Extraction', opts: ['They see the US-established \'rules-based\' international order as benefitting the West, especially the United States, over the emerging Global South.', 'They believe the Bretton Woods system gave far too much power and economic influence to the developing countries of the Global South.', 'They argue that the Bretton Woods era ended far too early and really should have continued running the world economy for much longer.', 'They claim the Bretton Woods rules forced Western countries to give unlimited amounts of free aid to every single BRICS member state.'], c: 0, fb: 'The source says BRICS+ criticise the rules-based order for favouring the West over the Global South. Option 2 reverses who benefits; option 3 defends the system; option 4 invents a free-aid rule.'},
  {id: 'q14', src: '3D', num: '3.4.3', m: 2, t: 'Using the information in the source and your own knowledge, explain why \'... the fact that the BRICS+ countries form 35% of the World\'s GDP and 45% of the world\'s population ...\' is a concern for the USA (Trump) as the leader of the Global North.', lv: 2, ll: 'Interpretation', opts: ['Such a large share of world GDP and population gives BRICS+ enormous economic and demographic weight, threatening US dominance and the global role of the dollar.', 'It is a concern only because those particular countries are located very far away from the United States and are difficult to trade with.', 'It is a concern because the Global North has an even larger and younger population that the BRICS+ bloc could never hope to match.', 'It is a concern because the BRICS+ countries had all promised to stop trading with one another completely by the year 2024.'], c: 0, fb: 'BRICS+ command of over a third of world GDP and nearly half its people threatens US dominance and the dollar. Option 2 is trivial; option 3 is false (the North is aging); option 4 invents a self-defeating trade halt.'},
  {id: 'q15', src: '3D', num: '3.4.4', m: 4, t: 'Comment on the usefulness of this source to a researcher studying how Trump felt threatened by the expansion of BRICS+ countries.', lv: 3, ll: 'Reliability', opts: ['It is very useful because it directly quotes Trump\'s own words at a 2024 rally, giving first-hand insight into how the leader of the Global North viewed the BRICS+ threat.', 'It is useful only because it is a very recent article, and any source that was published in the year 2024 must be completely accurate.', 'It is of very little use, because Trump was not involved in world affairs and had no real opinion at all about the BRICS+ countries.', 'It is useful because it provides detailed official economic statistics compiled by the International Monetary Fund on each of the BRICS+ economies.'], c: 0, fb: 'The source quotes Trump directly, giving first-hand insight into his views - exactly what the researcher needs. Option 2 wrongly equates recency with accuracy; option 3 is false (he was president-elect); option 4 describes Source 3C, not this opinion piece.'},
  {id: 'q16', src: 'compare', num: '3.5', m: 4, t: 'Refer to Sources 3C and 3D. Explain how the evidence in Source 3C supports the information in Source 3D regarding how BRICS is upsetting the balance of power between the Global North and Global South.', lv: 3, ll: 'Comparison', opts: ['Source 3C\'s graph shows BRICS\' GDP share rising to 32,1% while the G7\'s falls, and Source 3D confirms BRICS+ now form 35% of world GDP and are challenging the dollar.', 'The two sources contradict each other, because 3C shows BRICS steadily shrinking while 3D claims BRICS is one of the most powerful blocs in the whole world.', 'Source 3C measures only military spending while Source 3D measures only population growth, so the two sources cannot really be compared at all.', 'Both sources firmly agree that the G7 remains far wealthier than BRICS and that the global balance of power has not shifted in any way at all.'], c: 0, fb: '3C\'s rising BRICS GDP (32,1%) and 3D\'s figure of 35% of world GDP point the same way - a real power shift. Option 2 misreads 3C as shrinking; option 3 misdescribes both as unrelated; option 4 denies the shift both sources show.'},
  {id: 'q17', src: 'para', num: '3.6a', m: 2, t: 'STEP 1 - Topic sentence. Which sentence best opens a paragraph answering: how might the expansion of BRICS be seen as a threat to the balance of power between the Global North and Global South?', lv: 3, ll: 'Synthesis', opts: ['The expansion of BRICS threatens the balance of power by shifting economic and political weight from the Western Global North to the rising Global South.', 'The first BRIC meeting took place on 20 September 2006 on the side-lines of the UN General Assembly in New York.', 'The expansion of BRICS strengthened the G7\'s control over the world economy and confirmed Western dominance for many decades to come.', 'The Truth and Reconciliation Commission rejected the amnesty applications of the policemen who had tortured Reverend Tshenuwani Farisani.'], c: 0, fb: 'A topic sentence states the argument - here, a shift of power from North to South. Option 2 is a date fact; option 3 says the opposite; option 4 belongs to a completely different question (the TRC).'},
  {id: 'q18', src: 'para', num: '3.6b', m: 2, t: 'STEP 2 - Source evidence. Which option best uses specific evidence from the sources to support that topic sentence?', lv: 3, ll: 'Synthesis', opts: ['Source 3C shows BRICS\' GDP share rising to 32,1% as the G7\'s falls, and Sources 3B and 3D show BRICS pursuing de-dollarisation to weaken Western power.', 'Source 1D describes how the Security Branch demolished and then burned down COSATU House during a police raid in 1987.', 'Source 3A explains that the group was renamed from BRIC to BRICS after South Africa joined the association in 2011.', 'All of the sources agree that BRICS has no real economic power and poses no threat whatsoever to the Western countries.'], c: 0, fb: 'Strong evidence pairs the graph (3C) with de-dollarisation (3B, 3D). Option 2 is from a different question; option 3 is a true fact but shows no threat to the balance of power; option 4 contradicts the sources.'},
  {id: 'q19', src: 'para', num: '3.6c', m: 2, t: 'STEP 3 - Own knowledge. Which fact from your own knowledge best strengthens the paragraph beyond what the sources state?', lv: 3, ll: 'Synthesis', opts: ['BRICS set up its own New Development Bank as an alternative to the IMF and World Bank, cutting the South\'s dependence on the West.', 'Nelson Mandela was released from prison in 1990 and went on to become South Africa\'s first democratic president in 1994.', 'The graph in Source 3C was produced by Statista using IMF figures, exactly as that source already states.', 'The BRICS countries all agreed to adopt the US dollar as their single shared currency during the year 2024.'], c: 0, fb: 'The New Development Bank is real own knowledge that shows BRICS building rival institutions. Option 2 is off-topic; option 3 repeats the source rather than adding to it; option 4 is false (BRICS seeks de-dollarisation).'},
  {id: 'q20', src: 'para', num: '3.6d', m: 2, t: 'STEP 4 - Conclusion. Which sentence best concludes the paragraph by tying the evidence back to the question?', lv: 3, ll: 'Synthesis', opts: ['Because BRICS\' growing economic weight and its drive for de-dollarisation erode Western dominance, its expansion is a real threat to the North-South balance of power.', 'In conclusion, six new countries joined BRICS on 1 January 2024, among them Egypt, Iran and Saudi Arabia.', 'Overall, the sources prove that BRICS expansion has made no difference at all to the global balance of power.', 'To conclude, BRICS expansion was warmly welcomed by the G7 because it helped the West keep firm control of world trade.'], c: 0, fb: 'A conclusion draws the evidence together to answer the question, as option 1 does. Option 2 is a single fact; option 3 contradicts the evidence; option 4 is simply false.'},
];

const MTA_ANSWERS = [
  {label: 'Answer A', text: 'BRICS is getting bigger and this is a threat to the West. The graph in Source 3C shows that the BRICS share of GDP went up to 32,1% while the G7 went down. Source 3D says that BRICS+ make up 35% of the world\'s GDP. Trump was worried about this and said he would put a tariff on them. BRICS also wants to stop using the dollar. So BRICS is a threat to the balance of power between the Global North and the Global South.', expLv: 0, expM: '?-5', crit: [{q: 'Does it have a clear topic sentence addressing the question?', cor: 'partial', exp: 'The opening says BRICS is a threat to the West but does not clearly frame the answer around the shifting balance of power, so it only partly sets up the argument.'}, {q: 'Does it use specific evidence from the sources?', cor: 'yes', exp: 'It cites the 32,1% GDP figure from 3C, the 35% figure from 3D, Trump\'s tariff threat and de-dollarisation - concrete evidence from the sources.'}, {q: 'Does it include own knowledge beyond the sources?', cor: 'no', exp: 'Every point is drawn straight from the sources; there is no added fact such as the New Development Bank or the wider push for a multi-polar world.'}, {q: 'Does it explain WHY (analysis), not just WHAT (description)?', cor: 'partial', exp: 'It notes that Trump \'was worried\' but does not really explain why BRICS\' economic weight and de-dollarisation actually threaten Western dominance.'}, {q: 'Does it have a concluding sentence that synthesises?', cor: 'partial', exp: 'The final sentence restates that BRICS is a threat but does not pull the evidence together into a sharper judgement.'}, {q: 'Is it a coherent paragraph, not a list of disconnected sentences?', cor: 'yes', exp: 'The sentences follow on from one another and read as a connected paragraph rather than a disconnected list.'}]},
  {label: 'Answer B', text: 'The expansion of BRICS threatens the North-South balance of power by shifting economic weight from the West to the rising Global South. Source 3C shows BRICS\' share of global GDP climbing from 16,9% to 32,1% while the G7\'s falls to 29,6%, and Source 3D confirms that BRICS+ now form 35% of world GDP and 45% of its population. Sources 3A and 3B add that BRICS pursues de-dollarisation and an independent economic system to escape Western control. Beyond the sources, BRICS created its own New Development Bank as a rival to the IMF and World Bank. Because this steadily erodes US and G7 dominance, an expanding BRICS is a genuine threat to Western power.', expLv: 0, expM: '?-8', crit: [{q: 'Does it have a clear topic sentence addressing the question?', cor: 'yes', exp: 'The first sentence states the argument directly - expansion shifts power from North to South - answering exactly what the question asks.'}, {q: 'Does it use specific evidence from the sources?', cor: 'yes', exp: 'It cites 3C (16,9% to 32,1% versus the G7\'s 29,6%), 3D (35% of GDP, 45% of population) and 3B (de-dollarisation, an independent economic system), all precisely.'}, {q: 'Does it include own knowledge beyond the sources?', cor: 'yes', exp: 'It adds the New Development Bank as a rival to the IMF and World Bank, relevant own knowledge that none of the sources supply.'}, {q: 'Does it explain WHY (analysis), not just WHAT (description)?', cor: 'yes', exp: 'It explains the causal link: because BRICS\' weight and de-dollarisation erode US and G7 dominance, expansion is a genuine threat - reasoning, not just narration.'}, {q: 'Does it have a concluding sentence that synthesises?', cor: 'yes', exp: 'The closing sentence ties the erosion of Western dominance back to the question, synthesising the evidence into a clear answer.'}, {q: 'Is it a coherent paragraph, not a list of disconnected sentences?', cor: 'yes', exp: 'It flows logically from claim to source evidence to own knowledge to conclusion, reading as one connected argument.'}]},
];

/* ═══ KULA LEARN BRIDGE ═══
   Reports results to a parent Kula Learn page via postMessage.
   See project doc: Kula_Revise_results_payload_spec.md (spec v1).
   KR_META is injected by build.py from paper.json meta. The bridge is
   inert unless (a) identity fields are present, (b) the page is inside
   an iframe, and (c) the host origin was passed as ?host=<origin>.   */
const KR_META = {interactiveId: 'history-p2-2025-nov-q3', paper: 'P2', section: 'A', questionNumber: 3, paperRef: 'DBE November 2025'};
const KR_SPEC_VERSION = 1;
var krHostOrigin = '';
try {
    krHostOrigin = new URLSearchParams(location.search).get('host') || '';
} catch (e) {}
function krPost(type, payload) {
    if (!krHostOrigin || !KR_META || !KR_META.interactiveId || window.parent === window)
        return;
    try {
        window.parent.postMessage({
            source: 'kula-revise',
            specVersion: KR_SPEC_VERSION,
            type: type,
            payload: Object.assign({
                interactiveId: KR_META.interactiveId,
                paper: KR_META.paper,
                section: KR_META.section,
                questionNumber: KR_META.questionNumber,
                paperRef: KR_META.paperRef
            }, payload)
        }, krHostOrigin);
    } catch (e) {}
}
function krCompletedPayload() {
    const cappedEarM = Math.min(earM, totM);
    const p = totM ? Math.round(cappedEarM / totM * 100) : 0;
    const dbeLevel = p >= 80 ? 7 : p >= 70 ? 6 : p >= 60 ? 5 : p >= 50 ? 4 : p >= 40 ? 3 : p >= 30 ? 2 : 1;
    const lb = { 1: { e: 0, t: 0 }, 2: { e: 0, t: 0 }, 3: { e: 0, t: 0 } };
    QS.forEach(q => {
        lb[q.lv].t += q.m;
        if (ans[q.id] && ans[q.id].me)
            lb[q.lv].e += ans[q.id].me;
    });
    const wrong = QS.filter(q => ans[q.id] && ans[q.id].done && !ans[q.id].ok).map(q => ({
        id: q.id,
        num: q.num,
        level: q.lv,
        marks: q.m
    }));
    const slow = Object.entries(perQuestionTime).map(([id, t]) => {
        const q = QS.find(qq => qq.id === id);
        return q ? { num: q.num, seconds: t } : null;
    }).filter(Boolean).sort((a, b) => b.seconds - a.seconds).slice(0, 3);
    return {
        learnerName: learnerName || '',
        marksEarned: cappedEarM,
        marksTotal: totM,
        percent: p,
        dbeLevel: dbeLevel,
        levelBreakdown: {
            l1: { earned: lb[1].e, total: lb[1].t },
            l2: { earned: lb[2].e, total: lb[2].t },
            l3: { earned: lb[3].e, total: lb[3].t }
        },
        wrongQuestions: wrong,
        slowestQuestions: slow,
        totalTimeSeconds: Math.floor((Date.now() - totalStartTime) / 1000),
        completedAt: new Date().toISOString()
    };
}
var krCompletedSent = false;
var krMtaSubmitted = {};

// ═══ STATE ═══
let ans = {}
  , curQ = 0
  , totM = 0
  , earM = 0;
QS.forEach(q => {
    totM += q.m
}
);
let mtaState = {};
let learnerName = '';

// ═══ OPTION SHUFFLE (kills position bias: correct answer no longer always A/B) ═══
const ORIG_OPTS = QS.map(q => ({
    opts: q.opts.slice(),
    c: Array.isArray(q.c) ? q.c.slice() : q.c
}));
let optPerms = {};
const PERM_KEY = 'cwo-perms-v1';
function loadPerms() {
    try {
        const s = localStorage.getItem(PERM_KEY);
        if (s)
            optPerms = JSON.parse(s) || {}
    } catch (e) {
        optPerms = {}
    }
}
function savePerms() {
    try {
        localStorage.setItem(PERM_KEY, JSON.stringify(optPerms))
    } catch (e) {}
}
function applyShuffle() {
    QS.forEach( (q, qi) => {
        const orig = ORIG_OPTS[qi];
        if (!orig.opts || orig.opts.length < 2)
            return;
        let perm = optPerms[q.id];
        if (!perm || perm.length !== orig.opts.length) {
            perm = orig.opts.map( (_, i) => i);
            for (let i = perm.length - 1; i > 0; i--) {
                const j = Math.floor(Math.random() * (i + 1));
                [perm[i],perm[j]] = [perm[j], perm[i]];
            }
            optPerms[q.id] = perm;
        }
        const oldToNew = {};
        perm.forEach( (oi, ni) => {
            oldToNew[oi] = ni
        }
        );
        q.opts = perm.map(i => orig.opts[i]);
        if (Array.isArray(orig.c)) {
            q.c = orig.c.map(i => oldToNew[i]).sort( (a, b) => a - b);
        } else {
            q.c = oldToNew[orig.c];
        }
    }
    );
    savePerms();
}
loadPerms();
applyShuffle();
const NAME_KEY = 'cwo-name';
function firstName() {
    return (learnerName || '').trim().split(/\s+/)[0] || '';
}
function escHtml(s) {
    return String(s).replace(/[&<>"']/g, function(c) {
        return {
            '&': '&amp;',
            '<': '&lt;',
            '>': '&gt;',
            '"': '&quot;',
            "'": '&#39;'
        }[c]
    });
}

// ═══ RENDER ═══

// Add skill guide button for every question type
var SKILL_MAP = {
    'Extraction': {
        modal: 'extraction-modal',
        text: 'Not sure how to find the answer? Tap here for extraction tips'
    },
    'Concept': {
        modal: 'concept-modal',
        text: 'Need help defining concepts in your own words? Tap for guidance'
    },
    'Interpretation': {
        modal: 'interpretation-modal',
        text: 'Struggling to explain WHY, not just WHAT? Tap for interpretation tips'
    },
    'Analysis': {
        modal: 'analysis-modal',
        text: 'Not sure how to combine source + own knowledge? Tap for help'
    },
    'Reliability': {
        modal: 'reliability-modal',
        text: 'Not sure how to evaluate reliability? Tap for a step-by-step guide'
    },
    'Comparison': {
        modal: 'comparison-modal',
        text: 'Need help comparing sources? Tap for comparison tips'
    },
    'Synthesis': {
        modal: 'synthesis-modal',
        text: 'Need help building your paragraph? Tap for the step-by-step method'
    }
};
// ═══ SKILL GUIDE MODALS ═══
// Source: /History/G12 Skills/index.html SKILLS array; content adapted into modal markup
const SKILL_DATA = {
    extraction: {
        title: 'Extraction',
        sub: 'Finding information in sources',
        intro: 'Extraction questions ask you to <strong>find specific information that is directly stated in the source</strong>. The answer is IN the text, with no interpretation needed.',
        steps: [['Read the question first, then the source.', 'Underline the KEY WORDS. What exactly are you being asked to find? A person? A reason? A city? A date?'], ['Scan the source for those key words', 'or their synonyms. The answer is usually within one or two sentences of where the key words appear.'], ['Check your answer is actually IN the source.', 'If you can\'t point to the exact sentence, you\'re probably interpreting rather than extracting.'], ['Count carefully.', 'If the question says "identify TWO reasons," give exactly two. Missing one loses marks; adding extras wastes time.']],
        example: '<strong>Q:</strong> "Who replaced Byrnes as Secretary of State?" → Scan for "Byrnes" → "George Marshall" = 1 mark.',
        warn: 'Using your own knowledge instead of the source. Giving information that\'s true but not in THIS source. Wrong count of points.'
    },
    concept: {
        title: 'Concept Definition',
        sub: 'Explaining terms in your own words',
        intro: '"Define the concept… in your own words" tests whether you understand a term well enough to explain it <strong>without copying the source</strong>.',
        steps: [['Identify what KIND of thing it is.', 'Policy? Metaphor? Ideology? Process? Place it in a category first: "The Iron Curtain was a metaphor…"'], ['Explain what it means, not just what it is.', 'Say what it involved, who it affected, why it mattered. A definition that could apply to anything is too vague.'], ['Connect to the historical context', 'when the question says "in the context of." Link to the specific period, event, or situation.']],
        example: '<strong>Iron Curtain:</strong> A metaphor (category) for the dividing line separating communist Eastern Europe from capitalist Western Europe (meaning), after the USSR extended its sphere of influence post-1945 (context).',
        warn: 'Repeating the source word-for-word. Vague definitions without context. Confusing similar concepts (Iron Curtain vs Berlin Wall).'
    },
    interpretation: {
        title: 'Interpretation',
        sub: 'Explaining what evidence means and why it matters',
        intro: 'Interpretation questions ask you to go <strong>beyond what the source says</strong> to explain what it <strong>means</strong>, what it <strong>implies</strong>, or <strong>why it matters</strong>.',
        steps: [['Start with what the source SAYS, then explain what it MEANS.', 'Use bridge phrases: "This suggests…" / "This implies…" / "The significance is…"'], ['Ask WHY, not just WHAT.', 'Why did it happen? Why does it matter? What were the consequences?'], ['Consider language and purpose.', '"Armageddon" is much stronger than "important meeting". What does that word choice reveal?'], ['For "comment on" questions:', 'Interpret + explain significance. Connect to the bigger historical picture.']],
        example: '<strong>Weak:</strong> "Acheson called it \'Armageddon.\'" (just repeats source). <strong>Strong:</strong> Acheson used apocalyptic language because he saw the decision as existential, revealing the Cold War mentality where every decision was framed as good vs evil.',
        warn: 'Repeating the source without explaining meaning. If every sentence starts "The source says…" you\'re describing, not interpreting.'
    },
    analysis: {
        title: 'Source + Own Knowledge',
        sub: 'Combining evidence with what you\'ve studied',
        intro: 'These questions require <strong>two things</strong>: evidence from the source AND additional information from your own studies. You need both.',
        steps: [['Identify what the SOURCE tells you.', 'What evidence or clues does it provide? Start your answer by referring to the source.'], ['Add what you KNOW from your studies.', 'Additional facts, context, or background that helps explain what the source shows.'], ['LINK them together.', 'Show how your knowledge EXPLAINS what the source shows, or how the source CONFIRMS what you\'ve studied.']],
        example: '<strong>Source:</strong> Cartoon shows Greece/Turkey outside the Soviet bloc. <strong>Own K:</strong> The Truman Doctrine (1947) provided US aid to these countries. <strong>Link:</strong> The source shows the outcome; my knowledge explains the cause.',
        warn: 'Using only the source (no own knowledge). Using only own knowledge (no source). Listing both without connecting them.'
    },
    reliability: {
        title: 'Reliability & Bias',
        sub: 'Evaluating whether you can trust a source',
        intro: 'Reliability questions ask: <strong>can a historian trust this source?</strong> The answer is almost never simply "yes" or "no." Weigh strengths against limitations.',
        steps: [['WHO created it?', 'First-hand knowledge? Insider access? Eyewitness? Politician with an agenda?'], ['WHEN was it created?', 'During the events (immediate but less reflective) or after (more perspective, but shaped by hindsight)?'], ['WHY was it created?', 'To inform? Persuade? Justify? Rally support? Purpose shapes content.'], ['WHAT perspective does it represent?', 'Every source has a perspective. That doesn\'t make it unreliable, but a historian needs to be aware of it.']],
        example: '<strong>Churchill\'s Iron Curtain speech:</strong> Reliable because he had first-hand experience at Yalta/Potsdam (WHO) and the speech was contemporaneous (WHEN). However, his anti-communist stance and goal of rallying US support (WHY) mean the language may overstate the threat.',
        warn: '"Reliable because it\'s a primary source": primary sources can be biased. "Unreliable because biased": all sources have a perspective. Always weigh both sides.'
    },
    comparison: {
        title: 'Source Comparison',
        sub: 'Finding connections between two sources',
        intro: 'Comparison questions ask you to find <strong>connections between sources</strong>. How does one support, contradict, or extend another? Discuss BOTH together, not separately.',
        steps: [['Identify the THEME that connects them.', 'What topic or idea do both sources address? This is your bridge.'], ['Find specific EVIDENCE in each source', 'that relates to this shared theme. Concrete details from both, not vague summaries.'], ['Explain the CONNECTION.', 'Use linking phrases: "Source A states… and this is supported by Source B which shows…" / "Together, these sources demonstrate…"'], ['For 4-mark questions:', 'TWO comparison points, each with evidence from both sources and an explanation of the connection. 2×2 = 4 marks.']],
        example: '<strong>Theme:</strong> Both address Soviet expansion. <strong>1A:</strong> Churchill warns of Iron Curtain from Stettin to Trieste. <strong>1C:</strong> Acheson shows the threat materialising in Greece/Turkey. <strong>Link:</strong> 1C confirms and escalates the warning Churchill made in 1A.',
        warn: 'Describing each source separately without connections. Saying "the sources are similar" without explaining HOW. Comparing authors instead of content.'
    },
    synthesis: {
        title: 'Paragraph Writing',
        sub: 'Synthesising sources + own knowledge into an argument',
        intro: 'The paragraph question tests <strong>synthesis</strong>: weaving source evidence and your own knowledge into a coherent argument. It\'s assessed holistically.',
        steps: [['TOPIC SENTENCE:', 'Address the question directly in your first sentence. State your argument. Don\'t start with background; start with your answer.'], ['SOURCE EVIDENCE:', 'Use specific evidence from at least TWO sources. Reference them by number (Source 1A, Source 1C).'], ['OWN KNOWLEDGE:', 'Add at least ONE piece of information not in the sources. It must support your argument.'], ['CONCLUDING SENTENCE:', 'Return to the question. Name the mechanism: HOW and WHY, not just WHAT.']],
        example: '<strong>L3 (6–8):</strong> Clear topic sentence → specific source evidence woven in → own knowledge that extends the argument → conclusion that synthesises (shows HOW and WHY).',
        warn: 'A list of disconnected facts with no topic sentence. Naming sources without using their evidence. Missing own knowledge.'
    }
};

function injectSkillModals() {
    const host = document.getElementById('modals');
    if (!host || host.children.length)
        return;
    const slugFromMap = {};
    Object.keys(SKILL_MAP).forEach(k => {
        const slug = k.toLowerCase();
        const id = SKILL_MAP[k].modal;
        const dataKey = slug === 'synthesis' ? 'synthesis' : slug;
        const d = SKILL_DATA[dataKey];
        if (!d)
            return;
        const stepsHtml = d.steps.map( (s, i) => '<div class="skill-step"><span class="skill-step-num">' + (i + 1) + '</span><div class="skill-step-text"><strong>' + s[0] + '</strong> ' + s[1] + '</div></div>').join('');
        const html = '<div class="modal-overlay" id="' + id + '" onclick="closeSkillModal(event,\'' + id + '\')">' + '<div class="modal" onclick="event.stopPropagation()">' + '<div class="modal-hd"><span class="modal-title">' + d.title + '</span><button class="modal-close" aria-label="Close" onclick="closeSkillModal(null,\'' + id + '\')">×</button></div>' + '<div class="modal-body">' + '<h3>' + d.sub + '</h3>' + '<p>' + d.intro + '</p>' + stepsHtml + '<div class="skill-example"><strong>Example.</strong> ' + d.example + '</div>' + '<div class="skill-warn"><strong>Common mistakes.</strong> ' + d.warn + '</div>' + '</div>' + '</div></div>';
        host.insertAdjacentHTML('beforeend', html);
    }
    );
}
function closeSkillModal(e, id) {
    if (e && e.target !== e.currentTarget)
        return;
    const m = document.getElementById(id);
    if (m)
        m.classList.remove('show');
}
window.closeSkillModal = closeSkillModal;

function addSkillHint() {
    const q = QS[curQ];
    if (!q)
        return;
    var skill = SKILL_MAP[q.ll];
    if (!skill)
        return;
    injectSkillModals();
    // Find or create hint area
    var hintDiv = document.querySelector('.qp-hint');
    var panel = document.querySelector('.q-panel');
    if (!hintDiv) {
        // Create a hint div after qp-text
        var qt = panel.querySelector('.qp-text');
        hintDiv = document.createElement('div');
        hintDiv.className = 'qp-hint';
        hintDiv.innerHTML = '';
        qt.after(hintDiv);
    }
    if (!hintDiv.querySelector('.hint-btn')) {
        var btn = document.createElement('button');
        btn.className = 'hint-btn';
        btn.onclick = function(e) {
            e.stopPropagation();
            document.getElementById(skill.modal).classList.add('show')
        }
        ;
        btn.innerHTML = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width:18px;height:18px"><circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg> ' + skill.text + ' <span class="hint-pulse"></span>';
        hintDiv.appendChild(btn);
    }
}

// ═══ TIMER ═══
var qStartTime = 0
  , totalStartTime = Date.now()
  , qTimerInterval = null
  , totalTimerInterval = null;
var questionTimes = {};
// track actual time per question

function startQTimer() {
    qStartTime = Date.now();
    if (qTimerInterval)
        clearInterval(qTimerInterval);
    qTimerInterval = setInterval(updateQTimer, 1000);
    updateQTimer();
}

function updateQTimer() {
    const q = QS[curQ];
    if (!q)
        return;
    const elapsed = Math.floor((Date.now() - qStartTime) / 1000);
    const recommended = Math.max(30, (q.m || 1) * 45);
    const remaining = recommended - elapsed;
    const display = document.getElementById('q-timer');
    if (!display)
        return;

    if (remaining > 0) {
        const m = Math.floor(remaining / 60);
        const s = remaining % 60;
        display.textContent = m + ':' + (s < 10 ? '0' : '') + s;
        if (remaining > recommended * 0.3) {
            display.className = 'timer-display timer-ok';
        } else {
            display.className = 'timer-display timer-warn';
        }
    } else {
        const over = Math.abs(remaining);
        const m = Math.floor(over / 60);
        const s = over % 60;
        display.textContent = '+' + m + ':' + (s < 10 ? '0' : '') + s;
        display.className = 'timer-display timer-over';
    }
}

function stopQTimer() {
    if (qTimerInterval) {
        clearInterval(qTimerInterval);
        qTimerInterval = null
    }
    const q = QS[curQ];
    if (q)
        questionTimes[q.id] = Math.floor((Date.now() - qStartTime) / 1000);
}

function startTotalTimer() {
    totalStartTime = Date.now();
    totalTimerInterval = setInterval(updateTotalTimer, 1000);
}

function updateTotalTimer() {
    const elapsed = Math.floor((Date.now() - totalStartTime) / 1000);
    const m = Math.floor(elapsed / 60);
    const s = elapsed % 60;
    const display = document.getElementById('total-timer');
    if (display)
        display.textContent = (m < 10 ? '0' : '') + m + ':' + (s < 10 ? '0' : '') + s;
}

function getTotalTime() {
    const elapsed = Math.floor((Date.now() - totalStartTime) / 1000);
    const m = Math.floor(elapsed / 60);
    const s = elapsed % 60;
    return (m < 10 ? '0' : '') + m + ':' + (s < 10 ? '0' : '') + s;
}

function beginExam() {
    // capture name from input (required ≥2 chars)
    const inp = document.getElementById('learner-name');
    if (inp) {
        const v = (inp.value || '').trim();
        if (v.length < 2) {
            inp.focus();
            return;
        }
        learnerName = v;
        try {
            localStorage.setItem(NAME_KEY, learnerName);
        } catch (e) {}
    }
    document.getElementById('start-overlay').style.display = 'none';
    startTotalTimer();
    startQTimer();
    krPost('kr:started', {
        learnerName: learnerName || '',
        startedAt: new Date().toISOString()
    });
}
function onNameInput() {
    const inp = document.getElementById('learner-name');
    const btn = document.getElementById('begin-btn');
    if (!inp || !btn)
        return;
    btn.disabled = (inp.value || '').trim().length < 2;
}
window.onNameInput = onNameInput;

// ── Paragraph builder: live "your paragraph so far" panel + model comparison ──
function paraModelText() {
    if (typeof MTA_ANSWERS === 'undefined' || !MTA_ANSWERS || !MTA_ANSWERS.length)
        return '';
    var strong = MTA_ANSWERS.filter(function(a) {
        return (a.expM || '').indexOf('8') > -1;
    });
    var pick = strong.length ? strong[strong.length - 1] : MTA_ANSWERS[MTA_ANSWERS.length - 1];
    return pick ? pick.text : '';
}
function paraPanelHtml() {
    var ids = QS.filter(function(q) {
        return q.src === 'para';
    }).map(function(q) {
        return q.id;
    });
    if (!ids.length)
        return '';
    var chosen = [];
    ids.forEach(function(id) {
        var qq = QS.find(function(x) {
            return x.id === id;
        });
        var a = ans[id];
        if (qq && a && a.done && a.sel != null && !Array.isArray(a.sel))
            chosen.push(qq.opts[a.sel]);
    });
    if (!chosen.length)
        return '';
    var para = chosen.join(' ');
    var complete = chosen.length === ids.length;
    var onLast = QS[curQ].id === ids[ids.length - 1];
    var html = '<div class="para-build"><div class="para-build-h">&#9998; Your paragraph so far</div><div class="para-build-body">' + para + '</div>';
    if (!complete)
        html += '<div class="para-build-note">Keep choosing &mdash; each line you pick is added here, so you can watch your paragraph take shape rather than just picking options.</div>';
    html += '</div>';
    if (complete && onLast) {
        var model = paraModelText();
        if (model)
            html += '<div class="para-model"><div class="para-model-h">&#9733; A full-mark example</div><div class="para-model-body">' + model + '</div><div class="para-build-note">Compare this with the paragraph you built above. Notice how it weaves source evidence and your own knowledge into one flowing argument.</div></div>';
    }
    return html;
}

function render() {
    const q = QS[curQ];
    const s = SRC[q.src];
    const L = 'ABCDE';
    const isLast = curQ === QS.length - 1;
    const isFirst = curQ === 0;

    // Update top bar
    document.getElementById('tb-sec').textContent = s.label;
    document.getElementById('tb-qn').textContent = curQ + 1;
    document.getElementById('tb-qt').textContent = QS.length;
    const pct = Math.round(((curQ) / QS.length) * 100);
    document.getElementById('tb-fill').style.width = pct + '%';

    let html = `
  
  <div class="source-panel">
    <div class="sp-header"><span class="sp-label">${s.label}</span><span class="sp-type">${s.type}</span></div>
    <button class="sp-toggle" onclick="toggleSource(this)">▾ Hide source to focus on the question</button>
    <div class="sp-collapsible">${s.html}
    <div class="sp-attr">${s.attr}</div></div>
  </div>
  <div class="q-panel">
    <div class="qp-meta">
      <span class="qp-num">Q${q.num}</span>
      <span class="cc c${q.lv}">${q.ll}</span>
      <span class="qp-marks">${q.m} mark${q.m > 1 ? 's' : ''}</span>
    </div>
    <div class="qp-text">${q.t}</div>
    ${q.h ? `<div class="qp-hint">${q.h}</div>` : ''}
    <div class="ol" id="opts">${q.opts.map( (o, i) => `<button class="ob${ans[q.id]?.done ? (q.multi ? (q.c.includes(i) ? ' ok' : ((ans[q.id].sel || []).includes(i) ? ' no' : ' dim')) : (i === q.c ? ' ok' : (i === ans[q.id].sel ? ' no' : ' dim'))) : ((ans[q.id]?.sel != null && !q.multi && ans[q.id].sel === i) ? ' sel' : (q.multi && (ans[q.id]?.sel || []).includes(i) ? ' sel' : ''))}" data-i="${i}" ${ans[q.id]?.done ? 'disabled' : ''} onclick="${q.multi ? `tmm(${i},${q.mx})` : `sll(${i})`}"><span class="ol2">${L[i]}.</span><span style="flex:1">${o}</span><span class="om">${ans[q.id]?.done ? (q.multi ? (q.c.includes(i) ? '✓' : ((ans[q.id].sel || []).includes(i) ? '✗' : '')) : (i === q.c ? '✓' : (i === ans[q.id].sel ? '✗' : ''))) : ''}</span></button>`).join('')}</div>
    ${!ans[q.id]?.done ? `<div style="text-align:right;margin-top:.8rem"><button class="bs" id="sub-btn" onclick="submitQ()" ${ans[q.id]?.sel == null || (q.multi && (!ans[q.id]?.sel || ans[q.id].sel.length < q.mx)) ? 'disabled' : ''}>Submit</button></div>` : ''}
    <div class="fb ${ans[q.id]?.done ? ' show' : ''}${ans[q.id]?.ok ? ' fbg' : ''}${ans[q.id]?.done && !ans[q.id]?.ok ? ' fbb' : ''}" id="fb" aria-live="polite" role="status">${ans[q.id]?.done ? `<div class="fbl ${ans[q.id].ok ? 'flg' : 'flb'}">${ans[q.id].ok ? '✓ Correct, ' + q.m + ' mark' + (q.m > 1 ? 's' : '') + ' earned' : '✗ Not quite. 0 marks'}</div><div>${q.fb}</div>` : ''}</div>
    ${q.src === 'para' ? paraPanelHtml() : ''}
    <div class="nav-row">
      <button class="b2" onclick="prev()" ${isFirst ? 'disabled' : ''}>← Previous</button>
      ${isLast ? `<button class="bs" onclick="showResults()">View Results →</button>` : `<button class="bs" onclick="next()">Next →</button>`}
    </div>
  </div>`;

    document.getElementById('app').innerHTML = html;

    addSkillHint();
    // Mobile: start collapsed
    if (window.innerWidth < 600) {
        const col = document.querySelector('.sp-collapsible');
        const tgl = document.querySelector('.sp-toggle');
        if (col && !ans[q.id]?.done) {
            col.style.display = 'none';
            if (tgl)
                tgl.textContent = '▸ Show source';
        }
    }
    // Desktop: restore source panel scroll position between questions
    if (window.innerWidth >= 900) {
        const col = document.querySelector('.sp-collapsible');
        if (col && typeof __srcScroll === 'number')
            col.scrollTop = __srcScroll;
        if (col) {
            col.addEventListener('scroll', function() {
                __srcScroll = col.scrollTop
            });
        }
    }
    // restart per-question timer on every render that shows a new question
    if (__lastRenderedQ !== curQ) {
        if (__lastRenderedQ != null) {
            // accumulate time into perQuestionTime
            const prevQ = QS[__lastRenderedQ];
            if (prevQ) {
                const dt = Math.floor((Date.now() - qStartTime) / 1000);
                perQuestionTime[prevQ.id] = (perQuestionTime[prevQ.id] || 0) + dt;
            }
        }
        __lastRenderedQ = curQ;
        qStartTime = Date.now();
    }
    saveState();
}
var __lastRenderedQ = null;
var __srcScroll = 0;
var perQuestionTime = {};

function sll(i) {
    const q = QS[curQ];
    if (ans[q.id]?.done)
        return;
    ans[q.id] = ans[q.id] || {};
    ans[q.id].sel = i;
    render()
}
function tmm(i, mx) {
    const q = QS[curQ];
    if (ans[q.id]?.done)
        return;
    ans[q.id] = ans[q.id] || {};
    ans[q.id].sel = ans[q.id].sel || [];
    const a = ans[q.id].sel
      , p = a.indexOf(i);
    if (p > -1)
        a.splice(p, 1);
    else if (a.length < mx)
        a.push(i);
    render()
}

function submitQ() {
    stopQTimer();
    const q = QS[curQ];
    if (ans[q.id]?.done)
        return;
    let ok;
    if (q.multi) {
        const s = (ans[q.id].sel || []).sort()
          , c = [...q.c].sort();
        ok = s.length === c.length && s.every( (v, i) => v === c[i]);
    } else
        ok = ans[q.id].sel === q.c;
    ans[q.id].done = true;
    ans[q.id].ok = ok;
    ans[q.id].me = ok ? q.m : 0;
    // accumulate question time on submit
    const dt = Math.floor((Date.now() - qStartTime) / 1000);
    perQuestionTime[q.id] = (perQuestionTime[q.id] || 0) + dt;
    qStartTime = Date.now();
    uScore();
    saveState();
    render();
}

function next() {
    if (curQ < QS.length - 1) {
        curQ++;
        render();
        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        })
    }
}
function prev() {
    if (curQ > 0) {
        curQ--;
        render();
        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        })
    }
}

function uScore() {
    earM = 0;
    Object.values(ans).forEach(a => {
        if (a.done && a.me)
            earM += a.me
    }
    );
    const shown = Math.min(earM, totM);
    document.getElementById('tb-e').textContent = shown;
    const p = Math.min(100, Math.round(shown / totM * 100));
    document.getElementById('tb-fill').style.width = p + '%'
}

// ═══ MTA ═══
function goMTA() {
    stopQTimer();
    const s = SRC['mta'];
    let html = `<div class="source-panel">
    <div class="sp-header"><span class="sp-label">${s.label}</span><span class="sp-type">${s.type}</span></div>
    ${s.html}<div class="sp-attr">${s.attr}</div></div>
  <div class="q-panel" style="min-height:auto">`;

    MTA_ANSWERS.forEach( (a, ai) => {
        html += `<div style="margin:1.5rem 0">
    <div class="qp-meta"><span class="qp-num">${a.label}</span><span class="qp-marks" style="color:var(--gold);font-weight:700">Evaluate this answer</span></div>
    <div class="mta-box">${a.text}</div>
    <div id="rubric-${ai}">${a.crit.map( (cr, ci) => `
      <div class="rr"><span class="rl">${cr.q}</span>
      <div class="rbs">
        <button class="rb" onclick="markR(${ai},${ci},'yes')">Yes</button>
        <button class="rb" onclick="markR(${ai},${ci},'partial')">Partial</button>
        <button class="rb" onclick="markR(${ai},${ci},'no')">No</button>
      </div></div>
      <div class="fb" id="rfb-${ai}-${ci}"></div>`).join('')}</div>
    <div style="text-align:right;margin-top:.5rem"><button class="bs" id="mta-sub-${ai}" onclick="subMTA(${ai})">Check My Evaluation</button></div>
    <div class="fb" id="mta-fb-${ai}"></div></div>`;
    }
    );

    html += `<div class="nav-row">
    <button class="b2" onclick="curQ=QS.length-1;render();window.scrollTo({top:0,behavior:'smooth'})">← Back to questions</button>
    <button class="bs" onclick="showResults()">View Results →</button>
  </div></div>`;

    document.getElementById('app').innerHTML = html;
    document.getElementById('tb-sec').textContent = 'Mark This Answer';
    window.scrollTo({
        top: 0,
        behavior: 'smooth'
    });
}

function markR(ai, ci, val) {
    mtaState[ai] = mtaState[ai] || {};
    mtaState[ai][ci] = val;
    const row = document.querySelectorAll(`#rubric-${ai} .rr`)[ci];
    row.querySelectorAll('.rb').forEach(b => {
        b.classList.remove('ry', 'rn', 'rp');
        if (b.textContent.toLowerCase() === val)
            b.classList.add(val === 'yes' ? 'ry' : val === 'no' ? 'rn' : 'rp')
    }
    );
}

var bonusMarks = 0;
function subMTA(ai) {
    const a = MTA_ANSWERS[ai];
    const state = mtaState[ai] || {};
    let score = 0;
    a.crit.forEach( (cr, ci) => {
        const uv = state[ci] || '';
        const ok = uv === cr.cor;
        if (ok)
            score++;
        const fb = document.getElementById(`rfb-${ai}-${ci}`);
        fb.className = `fb show ${ok ? 'fbg' : 'fbb'}`;
        fb.innerHTML = `<div class="fbl ${ok ? 'flg' : 'flb'}">${ok ? '✓ Correct, 1 bonus mark' : '✗ The answer is: ' + cr.cor}</div><div>${cr.exp}</div>`;
    }
    );
    bonusMarks += score;
    const mfb = document.getElementById(`mta-fb-${ai}`);
    mfb.className = 'fb show fbg';
    mfb.innerHTML = `<div class="fbl flg">${score}/${a.crit.length} correct: ${score} bonus marks earned</div><div>This answer would earn approximately <strong>${ai === 0 ? '4–5 marks (Level 2)' : '7–8 marks (Level 3)'}</strong>. ${ai === 0 ? 'This answer shows understanding and uses source references, but lacks the analytical depth and clear causal argument needed for top marks.' : 'Strong synthesis: source evidence and own knowledge woven into a coherent argument with a clear line of reasoning.'}</div>`;
    document.getElementById(`mta-sub-${ai}`).classList.add('hidden');
    document.getElementById('tb-e').textContent = earM + (bonusMarks > 0 ? ' +' + bonusMarks + ' bonus' : '');
    krMtaSubmitted[ai] = true;
    if (Object.keys(krMtaSubmitted).length >= MTA_ANSWERS.length) {
        krPost('kr:bonus-completed', {
            learnerName: learnerName || '',
            bonusMarks: bonusMarks,
            bonusCompletedAt: new Date().toISOString()
        });
    }
}

// ═══ RESULTS ═══
function showResults() {
    stopQTimer();
    if (totalTimerInterval) {
        clearInterval(totalTimerInterval);
        totalTimerInterval = null;
    }
    if (!krCompletedSent) {
        krCompletedSent = true;
        try {
            krPost('kr:completed', krCompletedPayload());
        } catch (e) {}
    }
    const cappedEarM = Math.min(earM, totM);
    const p = Math.round(cappedEarM / totM * 100);
    let g, gc, cm;
    if (p >= 80) {
        g = 'Level 7';
        gc = 'var(--ok)';
        cm = 'Outstanding. Thorough understanding of source analysis, interpretation, evaluation and synthesis.';
    } else if (p >= 70) {
        g = 'Level 6';
        gc = 'var(--ok)';
        cm = 'Meritorious. Strong source skills; refine evaluation and comparison for full marks.';
    } else if (p >= 60) {
        g = 'Level 5';
        gc = 'var(--blue-lt)';
        cm = 'Substantial. You handle extraction and interpretation well. Develop your evaluation and synthesis skills.';
    } else if (p >= 50) {
        g = 'Level 4';
        gc = 'var(--warn)';
        cm = 'Adequate. Extraction is solid. Focus on moving from WHAT to WHY: interpretation and evaluation.';
    } else if (p >= 40) {
        g = 'Level 3';
        gc = 'var(--warn)';
        cm = 'Moderate. You can find information but struggle with interpretation. Ask: what does this evidence MEAN?';
    } else if (p >= 30) {
        g = 'Level 2';
        gc = 'var(--bad)';
        cm = 'Elementary. Start with careful source reading and extraction before attempting higher-order questions.';
    } else {
        g = 'Level 1';
        gc = 'var(--bad)';
        cm = 'Not yet achieved. Focus on reading sources carefully to find specific information.';
    }

    let lb = {
        1: {
            e: 0,
            t: 0
        },
        2: {
            e: 0,
            t: 0
        },
        3: {
            e: 0,
            t: 0
        }
    };
    QS.forEach(q => {
        lb[q.lv].t += q.m;
        if (ans[q.id]?.me)
            lb[q.lv].e += ans[q.id].me
    }
    );

    const fn = firstName();
    const heading = fn ? 'Well done, ' + escHtml(fn) : 'Examination Complete';
    document.getElementById('app').innerHTML = `
  <div style="padding:1rem">
  <div class="rc">
    <div class="hero-ey" style="color:var(--red)">${heading}</div>
    <div class="rs-num" style="color:${gc}">${cappedEarM}</div>
    <div class="rt">out of ${totM} marks, ${p}%</div>
    <div style="font-family:'Playfair Display',serif;font-size:1.4rem;font-weight:700;color:${gc};margin-top:.8rem">${g}</div>
    <p style="margin-top:.6rem;font-size:.88rem;color:var(--txt2);max-width:500px;margin:auto">${cm}</p>
    <div style="margin-top:1rem;padding:.7rem 1.1rem;background:#FEF9E7;border:1px solid #D4A745;border-radius:4px;display:inline-block"><span style="font-size:.62rem;text-transform:uppercase;letter-spacing:.18em;font-weight:700;color:#D4A745">Bonus &middot; Mark This Answer</span><br><span style="font-family:Playfair Display,serif;font-size:1.6rem;font-weight:700;color:#D4A745">+${bonusMarks}</span><span style="font-size:.8rem;color:#5D6D7E"> / 12 extra</span></div>
  </div>
  <div style="text-align:center;margin-top:1.5rem">
    <button class="bs" onclick="location.reload()">Try Again</button>
    ${p >= 70 ? `<div style="margin-top:1.5rem;padding:1.2rem;background:linear-gradient(135deg,#1B4F72,#2E86C1);border-radius:6px;text-align:center"><div style="font-size:.7rem;text-transform:uppercase;letter-spacing:.25em;color:#D4A745;font-weight:700;margin-bottom:.4rem">★ Bonus Section Unlocked</div><div style="color:#fff;font-size:.95rem;margin-bottom:.8rem">You scored ${p}%. Well done! You've unlocked the bonus round: mark two sample paragraph answers like an examiner to earn up to 12 bonus marks.</div><button class="bs" style="background:#D4A745;color:#1C2733" onclick="goMTA()">Start Bonus Section →</button></div>` : `<div style="margin-top:1rem;padding:1rem;background:var(--frost);border:1px solid var(--bdr);border-radius:4px"><div style="font-size:.82rem;color:var(--txt2)"><strong>Bonus round locked.</strong> Score 70% or higher to unlock it and earn up to 12 extra marks by marking two sample answers like an examiner.</div></div>`}
    </div>
  </div>`;
    // ── Level breakdown bars + per-question time analytics + review-wrong button ──
    try {
        const wrong = QS.filter(q => ans[q.id] && ans[q.id].done && !ans[q.id].ok);
        // top-time questions
        const times = Object.entries(perQuestionTime).map( ([id,t]) => {
            const q = QS.find(qq => qq.id === id);
            return q ? {
                num: q.num,
                t: t
            } : null;
        }
        ).filter(Boolean).sort( (a, b) => b.t - a.t).slice(0, 3);
        const fmtT = s => {
            const m = Math.floor(s / 60)
              , ss = s % 60;
            return (m > 0 ? m + 'm ' : '') + ss + 's';
        }
        ;
        const extra = document.createElement('div');
        extra.className = 'si';
        extra.style.cssText = 'background:var(--white);border:1px solid var(--bdr);border-radius:4px;margin:1rem';
        extra.innerHTML = '<h2>Level Score Bars</h2>' + [{
            l: 'L1: Extraction/Concept',
            d: lb[1]
        }, {
            l: 'L2: Interpretation/Own knowledge',
            d: lb[2]
        }, {
            l: 'L3: Analysis/Comparison/Reliability',
            d: lb[3]
        }].map(x => {
            const pct = x.d.t ? Math.round(x.d.e / x.d.t * 100) : 0;
            const col = pct >= 70 ? 'var(--ok)' : pct >= 40 ? 'var(--warn)' : 'var(--bad)';
            return '<div class="lvl-bar"><span class="ll">' + x.l + '</span><span class="lt"><span class="lt-f" style="width:' + pct + '%;background:' + col + '"></span></span><span class="lv">' + x.d.e + '/' + x.d.t + ' · ' + pct + '%</span></div>';
        }
        ).join('') + (times.length ? ('<h2 style="margin-top:1rem">Slowest Questions</h2><div class="time-list">' + times.map(t => '<div class="ti"><span>Q' + t.num + '</span><span>' + fmtT(t.t) + '</span></div>').join('') + '</div>') : '') + (wrong.length ? ('<div style="text-align:center;margin-top:1rem"><button class="bs" onclick="reviewWrong()">Review Wrong Answers (' + wrong.length + ')</button></div>') : '');
        document.querySelector('#app > div').appendChild(extra);
    } catch (e) {}
    // Clear persisted state: exam complete
    try {
        localStorage.removeItem('cwo-june2025-v1');
        localStorage.removeItem(PERM_KEY);
    } catch (e) {}
    // Add time to results
    var timeHtml = document.querySelector('.top-bar');
    if (timeHtml)
        timeHtml.classList.add('hidden');
    window.scrollTo({
        top: 0,
        behavior: 'smooth'
    });
}

// ═══ INIT ═══
document.getElementById('tb-t').textContent = totM;
document.getElementById('tb-qt').textContent = QS.length;

// Apply persisted theme BEFORE first render
(function initTheme() {
    try {
        const t = localStorage.getItem('cwo-theme');
        if (t === 'dark')
            document.documentElement.setAttribute('data-theme', 'dark');
    } catch (e) {}
}
)();

// Inject theme toggle into top bar
(function injectThemeToggle() {
    const bar = document.querySelector('.top-bar');
    if (!bar)
        return;
    let btn = document.getElementById('theme-toggle');
    if (!btn) {
        btn = document.createElement('button');
        btn.className = 'theme-toggle';
        btn.id = 'theme-toggle';
        btn.setAttribute('aria-label', 'Toggle dark mode');
        btn.title = 'Toggle dark mode';
        bar.appendChild(btn);
    }
    btn.textContent = document.documentElement.getAttribute('data-theme') === 'dark' ? '☀︎' : '☾';
    btn.onclick = function() {
        const isDark = document.documentElement.getAttribute('data-theme') === 'dark';
        if (isDark) {
            document.documentElement.removeAttribute('data-theme');
            btn.textContent = '☾';
        } else {
            document.documentElement.setAttribute('data-theme', 'dark');
            btn.textContent = '☀︎';
        }
        try {
            localStorage.setItem('cwo-theme', isDark ? 'light' : 'dark');
        } catch (e) {}
    }
    ;
}
)();

// ── Persistence: save/load state ──
const STORE_KEY = 'cwo-june2025-v1';
function saveState() {
    try {
        // accumulate current question time before saving
        const liveDt = (qStartTime ? Math.floor((Date.now() - qStartTime) / 1000) : 0);
        localStorage.setItem(STORE_KEY, JSON.stringify({
            ans: ans,
            curQ: curQ,
            perQuestionTime: perQuestionTime,
            bonusMarks: bonusMarks,
            mtaState: mtaState,
            totalElapsedAt: Date.now(),
            totalStartTime: totalStartTime,
            version: 1
        }));
    } catch (e) {}
}
function loadState() {
    try {
        const raw = localStorage.getItem(STORE_KEY);
        if (!raw)
            return null;
        const o = JSON.parse(raw);
        if (!o || o.version !== 1)
            return null;
        return o;
    } catch (e) {
        return null;
    }
}
function clearState() {
    try {
        localStorage.removeItem(STORE_KEY);
        localStorage.removeItem(PERM_KEY);
    } catch (e) {}
}

// Patch sll/tmm/next/prev/markR/subMTA to persist
(function patchPersist() {
    const _sll = sll
      , _tmm = tmm
      , _next = next
      , _prev = prev
      , _markR = markR
      , _subMTA = subMTA;
    sll = function(i) {
        _sll(i);
        saveState();
    }
    ;
    tmm = function(i, mx) {
        _tmm(i, mx);
        saveState();
    }
    ;
    next = function() {
        _next();
        saveState();
    }
    ;
    prev = function() {
        _prev();
        saveState();
    }
    ;
    markR = function(a, c, v) {
        _markR(a, c, v);
        saveState();
    }
    ;
    subMTA = function(a) {
        _subMTA(a);
        saveState();
    }
    ;
    // expose globally
    window.sll = sll;
    window.tmm = tmm;
    window.next = next;
    window.prev = prev;
    window.markR = markR;
    window.subMTA = subMTA;
}
)();

// ── Source toggle handler (replaces inline onclick string) ──
function toggleSource(btn) {
    const col = btn.nextElementSibling;
    if (!col)
        return;
    const isHidden = col.style.display === 'none';
    col.style.display = isHidden ? 'block' : 'none';
    btn.textContent = isHidden ? '▾ Hide source to focus on the question' : '▸ Show source';
}
window.toggleSource = toggleSource;

// ── Review-wrong mode ──
var reviewMode = false
  , reviewQueue = []
  , reviewIdx = 0
  , _origCurQ = null;
function reviewWrong() {
    const wrongIdx = QS.map( (q, i) => ({
        q,
        i
    })).filter(x => ans[x.q.id] && ans[x.q.id].done && !ans[x.q.id].ok).map(x => x.i);
    if (!wrongIdx.length)
        return;
    reviewMode = true;
    reviewQueue = wrongIdx;
    reviewIdx = 0;
    _origCurQ = curQ;
    // reset done flag so the question can be re-attempted; preserve original record
    wrongIdx.forEach(i => {
        const id = QS[i].id;
        if (ans[id]) {
            ans[id]._prev = Object.assign({}, ans[id]);
            delete ans[id].done;
            delete ans[id].ok;
            delete ans[id].me;
            delete ans[id].sel;
        }
    }
    );
    curQ = reviewQueue[reviewIdx];
    // restore top bar if hidden
    const tb = document.querySelector('.top-bar');
    if (tb)
        tb.classList.remove('hidden');
    render();
}
window.reviewWrong = reviewWrong;

// ── Keyboard shortcuts ──
document.addEventListener('keydown', function(e) {
    // ignore when typing in input/textarea
    const tag = (e.target && e.target.tagName || '').toLowerCase();
    if (tag === 'input' || tag === 'textarea' || e.target.isContentEditable)
        return;
    // ignore when start overlay is showing or any modal is open
    const startVis = document.getElementById('start-overlay');
    if (startVis && startVis.style.display !== 'none')
        return;
    if (document.querySelector('.modal-overlay.show'))
        return;
    // number keys 1-9: select option
    if (e.key >= '1' && e.key <= '9') {
        const idx = parseInt(e.key, 10) - 1;
        const opts = document.querySelectorAll('#opts .ob');
        if (opts[idx] && !opts[idx].disabled) {
            opts[idx].click();
            e.preventDefault();
        }
        return;
    }
    if (e.key === 'Enter') {
        const sub = document.getElementById('sub-btn');
        if (sub && !sub.disabled) {
            sub.click();
            e.preventDefault();
            return;
        }
        const q = QS[curQ];
        if (q && ans[q.id] && ans[q.id].done) {
            // advance
            if (curQ < QS.length - 1) {
                next();
                e.preventDefault();
            }
        }
        return;
    }
    if (e.key === 'ArrowLeft') {
        if (curQ > 0) {
            prev();
            e.preventDefault();
        }
    } else if (e.key === 'ArrowRight') {
        const q = QS[curQ];
        if (q && ans[q.id] && ans[q.id].done && curQ < QS.length - 1) {
            next();
            e.preventDefault();
        }
    }
});

// ── beforeunload guard ──
window.addEventListener('beforeunload', function(e) {
    const anyAnswered = Object.values(ans).some(a => a && a.done);
    const completed = document.querySelector('.rc') != null;
    if (anyAnswered && !completed) {
        e.preventDefault();
        e.returnValue = '';
        return '';
    }
});

// ── Service worker registration + auto-reload when a fresh version takes over ──
if ('serviceWorker'in navigator) {
    try {
        navigator.serviceWorker.register('sw.js').catch(function() {});
        // When a new SW takes control, reload once so users immediately see the new code.
        let _reloaded = false;
        navigator.serviceWorker.addEventListener('message', function(ev) {
            if (ev.data && ev.data.type === 'SW_UPDATED' && !_reloaded) {
                _reloaded = true;
                location.reload();
            }
        });
    } catch (e) {}
}

// ── Resume / fresh-start flow ──
(function startFlow() {
    // Load persisted name and prefill input
    try {
        const n = localStorage.getItem(NAME_KEY);
        if (n) {
            learnerName = n;
        }
    } catch (e) {}
    // Optional: host (Kula Learn) can personalise by passing ?name=... in the iframe URL.
    // The learner is never asked to type their name; if no name is supplied, greetings stay generic.
    try {
        const qn = new URLSearchParams(location.search).get('name');
        if (qn && qn.trim()) {
            learnerName = qn.trim();
        }
    } catch (e) {}
    const nameInp = document.getElementById('learner-name');
    if (nameInp) {
        if (learnerName) {
            nameInp.value = learnerName;
        }
        onNameInput();
    }
    const saved = loadState();
    const overlay = document.getElementById('start-overlay');
    // expose original beginExam, then replace
    const _beginExam = window.beginExam;
    window.beginExam = function() {
        clearState();
        ans = {};
        curQ = 0;
        earM = 0;
        perQuestionTime = {};
        mtaState = {};
        bonusMarks = 0;
        __lastRenderedQ = null;
        _beginExam();
        render();
    }
    ;
    window.resumeExam = function() {
        if (saved) {
            ans = saved.ans || {};
            curQ = saved.curQ || 0;
            perQuestionTime = saved.perQuestionTime || {};
            mtaState = saved.mtaState || {};
            bonusMarks = saved.bonusMarks || 0;
        }
        overlay.style.display = 'none';
        startTotalTimer();
        startQTimer();
        uScore();
        render();
    }
    ;
    window.startOver = function() {
        clearState();
        window.beginExam();
    }
    ;

    if (saved && Object.keys(saved.ans || {}).length > 0) {
        // Show resume prompt by injecting into existing start overlay
        const bd = overlay.querySelector('.sm-bd');
        if (bd) {
            const answered = Object.values(saved.ans).filter(a => a && a.done).length;
            const resumeBox = document.createElement('div');
            resumeBox.className = 'coach-box';
            resumeBox.style.background = 'linear-gradient(135deg,#1B4F72,#2E86C1)';
            const fn = firstName();
            const welcome = fn ? ('Welcome back, ' + escHtml(fn) + '. Resume your previous attempt?') : 'Resume your previous attempt?';
            resumeBox.innerHTML = '<div class="coach-title">' + welcome + '</div>' + '<p>You answered <strong>' + answered + '</strong> question' + (answered !== 1 ? 's' : '') + ' last time. Pick up where you left off, or start fresh.</p>' + '<div style="display:flex;gap:.5rem;margin-top:.6rem"><button class="bs" style="flex:1" onclick="resumeExam()">Resume</button>' + '<button class="b2" style="flex:1;background:rgba(255,255,255,.1);color:#fff;border-color:rgba(255,255,255,.3)" onclick="startOver()">Start Over</button></div>';
            bd.insertBefore(resumeBox, bd.firstChild);
        }
    }
    // Keyboard shortcuts hint at bottom of overlay
    // (keyboard tip already lives in the static start-overlay markup — no second copy)
}
)();

render();
